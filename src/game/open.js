export const open = () => {
  const game = document.querySelector(".game");
  const console = document.querySelector('[data-type="g03.jpg"]');

  const from = console.getBoundingClientRect();
  const to = game.getBoundingClientRect();
};
